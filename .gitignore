# Temporary research clones
.tmp-openai-plugins/
.tmp-upstream/

# Build output (regenerated by `node scripts/typecheck.mjs` / tsc)
packages/dsh-cloudflare/lib/
packages/dsh-cloudflare/tsconfig.tsbuildinfo

# Editor/OS
node_modules/
*.tsbuildinfo

# Local npm cache (sandbox workaround; never commit)
.npm-cache/

# Package-level install (for building standalone)
packages/dsh-cloudflare/node_modules/
packages/dsh-cloudflare/package-lock.json

# Recursive-mode runtime state (written by dsh-recursive-mode into the workspace)
.recursive/
